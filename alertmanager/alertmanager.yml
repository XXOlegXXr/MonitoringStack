route:
  receiver: 'telegram'
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: 'telegram'
    webhook_configs:
      - url: 'http://telegram-relay:8000/notify/alerts'
        send_resolved: true
